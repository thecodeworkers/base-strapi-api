apiVersion: v1
kind: Service
metadata:
  name: base-dev-api-service
  namespace: strapi-base
  labels:
    app: base-dev-api
spec:
  ports:
    - name: base-dev-api
      port: 1337
  selector:
    app: base-dev-api
